import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#00f100',
      contrastText: '#000000',
    },
    secondary: {
      main: '#f50057',
      contrastText: '#ffffff',
    },
  },
});
  